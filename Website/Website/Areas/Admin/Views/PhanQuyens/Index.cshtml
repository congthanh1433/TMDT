@model IEnumerable<Website.Models.PhanQuyen>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Layout/HomeLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LoaiQuyen)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LoaiQuyen)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaPQ }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaPQ }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaPQ })
        </td>
    </tr>
}

</table>
